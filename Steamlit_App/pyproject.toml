# This is the fix. Just list "pandas".
[project]
name = "app"
version = "0.1.0"
description = "A simple Streamlit app to view local Excel files."

dependencies = [
    "streamlit",
    "pandas",     # <-- This will get a modern, compatible version
    "openpyxl"
]
